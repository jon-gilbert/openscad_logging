NOMATCH_TEXT="ECHO: "

SCAD=`cat <<EOM
    include <logging.scad>
    //LOG_LEVEL=false;
    d = log_debug("debug");
    i = log_info("info");
    w = log_warning("warning");
    e = log_error("error");
    //f = log_fatal("fatal");
    assert(d == undef);
    assert(i == undef);
    assert(w == undef);
    assert(e == undef);
    //assert(f == "fatal");
EOM
`

